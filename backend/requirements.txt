# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9

# --- Database ---
sqlalchemy>=2.0
psycopg[binary]>=3.1
geoalchemy2>=0.15

# --- Async processing ---
celery[redis]>=5.3
redis>=5.0

# --- Object storage (MinIO / S3) ---
minio>=7.2

# --- Geo / conversion engine (manylinux wheels bundle GDAL/GEOS/PROJ) ---
rasterio>=1.3
rio-cogeo>=5.0
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
fiona>=1.9
numpy>=1.26

# Note: pins are lower bounds for the scaffold. Run `pip freeze > requirements.lock`
# after the first successful build to lock exact versions.
